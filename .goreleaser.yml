version: 2
before:
  hooks:
    - go mod tidy
builds:
  - main: ./cmd/awsdag
    ldflags:
      - -X main.version={{.Version}}
    env:
      - CGO_ENABLED=0
checksum:
  name_template: "checksums.txt"
archives:
  - format_overrides:
      - goos: windows
        formats: [zip]
